{
  "name": "analyze-transcript-lambda",
  "version": "1.0.0",
  "description": "Lambda function for analyzing transcripts using OpenAI",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp dist/index.js index.js",
    "clean": "rm -rf dist index.js",
    "prebuild": "npm run clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
