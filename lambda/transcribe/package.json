{
  "name": "transcribe-lambda",
  "version": "1.0.0",
  "description": "Lambda function for transcribing audio files using OpenAI Whisper",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "node-fetch": "^2.7.0",
    "openai": "^4.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "@types/node-fetch": "^2.6.2",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.0.0"
  }
}
